Follow the builder instructions in `agents/builder.md`.

Read `website/brief.md` (required) and `website/research.md` (if it exists).

Generate the complete website:
- `website/index.html`
- `website/styles.css`
- `website/script.js`

Use only real content from the brief and research. No placeholders.

After generating, run `sh scripts/check.sh` and report results. Tell the user to open http://localhost:8000 if the preview server is running.
